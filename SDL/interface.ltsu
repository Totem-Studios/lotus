@seize
@def STRING char *
@def print shell::log

namespace shell {
    fn log(STRING format, ...) {

    }: 0;

    fn write(STRING format, ...) {

    }: 0;

    fn note(STRING format, ...) {

    }: 0;
}