fn sum(x: i32, y: i32) -> i32 {
    x = x * 2;
    ret x + y;
}

fn main() -> i32 {
    foo: i32 = 10;
    bar: i32;
    bar = 20;
    baz := bar + foo;
    ret sum(foo, baz);
}
// returns 50