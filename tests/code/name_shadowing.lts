fn main() -> i32 {
    x: i32 = 10;
    {
        x: i32 = 50;
        {
            x: i32 = 100;
            printf("x is %d\n", x);
        }
        printf("x is %d\n", x);
    }
    printf("x is %d", x);
    ret 0;
}

// output:
// x is 100
// x is 50
// x is 10